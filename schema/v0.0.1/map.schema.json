{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "${{BASE_URL}}/schema/${{VERSION}}/map.schema.json",
  "title": "haptic_game_integration",
  "description": "Defines how and where haptic inputs are coming from in a game.",
  "type": "object",
  "properties": {
    "schemaVersion": {
      "type": "string",
      "description": "What version this conforms to.",
      "const": "v0.0.1"
    },
    "identification": {
      "description": "A group of defining features that are used to load configurations when an avatar is detected. All maps with the same name are expected to be compatible, with the latest as a default.",
      "type": "object",
      "properties": {
        "authorName": {
          "description": "The name of the author of the map, all map names must be unique for a author. Please use a screen name",
          "type": "string"
        },
        "mapName": {
          "description": "The name of this map. usually some identifying information",
          "type": "string"
        },
        "mapVersion": {
          "description": "Increments with each version change.",
          "type": "integer"
        },
        "authorId": {
          "description": "Author UUID assigned by the official distribution system. Work in Progress, currently does nothing",
          "type": "string"
        },
        "customRepository": {
          "description": "Custom github repository that will be used to look for newer versions or patches of this map. Only Github pages with default domains are accepted for some semblance of security",
          "type": "object",
          "properties": {
            "author": {
              "type": "string"
            },
            "repository": {
              "type": "string"
            },
            "indexFile": {
              "description": "URL path from the gh-pages base to your index.json file. URL path must end in index.json",
              "type": "string"
            }
          }
        }
      },
      "required": ["authorName", "mapName", "mapVersion"]
    },
    "nodes": {
      "description": "Nodes that represent how addresses should be mapped to locations in space.",
      "type": "array",
      "items": {
        "$ref": "${{BASE_URL}}/schema/${{VERSION}}/node.schema.json"
      },
      "uniqueItems": true,
      "minItems": 1
    }
  },
  "required": ["identification", "nodes", "schemaVersion"]
}
